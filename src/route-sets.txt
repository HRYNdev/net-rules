# Наборы, по которым клиент гонит трафик в тоннель (route.rule_set в конфиге).
# Лежат на сервере подписки, в этом репозитории их нет, поэтому проверка
# скачивает их оттуда. Одна строка - одно имя файла без .srs.
#
# Сверено 13.08.2026: это про КЛИЕНТА. Дома тракт другой - на Pi sing-box берёт
# те же наборы НАПРЯМУЮ с itdoginfo (`/etc/sing-box/config.json`, url'ы ведут на
# github.com/itdoginfo/allow-domains), а не с нашей раздачи. Файлы те же самые,
# наш sync-rules.sh кладёт их копии в subkv/rules/community, поэтому проверка
# конфликтов смотрит на верное содержимое. Разница только в одном: дома страховка
# «источник молчит - оставляем прежнее» не работает, там её держит сам podkop.
anime
cloudflare
cloudfront
digitalocean
discord
geoblock
google_ai
google_play
hdrezka
hetzner
hodca
meta
ovh
roblox
russia_inside
telegram
tiktok
twitter
youtube
